import { styled } from '@mui/material/styles';

import { Link } from 'react-router-dom';

const ROUTER_LINK___STYLED = styled(Link)(
    ({ theme }) => `

  text-decoration: none;
  color: inherit;
  cursor: pointer;
  
  `
)

export default ROUTER_LINK___STYLED